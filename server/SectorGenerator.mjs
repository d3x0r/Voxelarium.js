
const fillers = [
	fillBlackRockBlue,
	fillBlackRockBlue,
	fillBlackRockGreen,
	fillBlackRockGreen,
];

class Generator {
	constructor()
	{
	}

	fillSector( s ) {
		const l = Math.abs(s.x)+Math.abs(s.y)+Math.abs(s.z);
		filler = fillers[l];
	}
}

function fillBlackRockBlue(s) {
	if( z < 0 ) {
		
	}
}

